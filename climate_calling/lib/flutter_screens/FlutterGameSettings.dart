import "package:flutter/material.dart";

class FlutterGameSettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //TODO: replace with widget tree for game settings screen
    return Container();
  }
}
